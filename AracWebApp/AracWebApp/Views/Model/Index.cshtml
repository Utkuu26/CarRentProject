@model IEnumerable<AracWebApp.Models.tblModel>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.modelAd)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.tblMarka.markaAd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.modelAd)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.tblMarka.markaAd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.modelId }) |
            @Html.ActionLink("Details", "Details", new { id=item.modelId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.modelId })
        </td>
    </tr>
}

</table>
