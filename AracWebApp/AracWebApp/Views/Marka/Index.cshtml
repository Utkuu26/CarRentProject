@model IEnumerable<AracWebApp.Models.tblMarka>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.markaAd)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.markaAd)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.markaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.markaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.markaId })
        </td>
    </tr>
}

</table>
